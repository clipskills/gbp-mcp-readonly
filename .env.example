# Google OAuth (später ausfüllen)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://YOUR-CLOUD-RUN-URL.a.run.app/oauth/callback

# MCP Bearer Token (später ausfüllen)
MCP_BEARER_TOKEN=

# Cloud Run (bleibt so)
PORT=8080
MCP_HTTP_HOST=0.0.0.0
